1. Two pointer technique used to find pair of elements in array whose sum = x(can be applied only when array is sorted)
Note: Quick sort worst case TC: n sq while merge and heap has nlogn in worst case too.
2. Above point can be applied  on sorted rotated array too.
3.